import { Routes } from '@angular/router';
import { ScheduleViewComponent } from './views/schedule-view/schedule-view.component';

export const SCHEDULE_ROUTES: Routes = [{ path: '', component: ScheduleViewComponent }];
